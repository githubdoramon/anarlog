"$schema" = "schemas/schema.json"

[default]
description = "Default permissions for db commands."
permissions = [
  "allow-execute",
  "allow-execute-proxy",
  "allow-subscribe",
  "allow-unsubscribe",
]
